FROM alpine:3.19

RUN apk add --no-cache sqlite dcron

COPY backup-cron.sh /usr/local/bin/backup-cron.sh
RUN chmod +x /usr/local/bin/backup-cron.sh

# Add cron job to run daily at 3:00 AM
RUN echo "0 3 * * * /usr/local/bin/backup-cron.sh >> /var/log/backup.log 2>&1" | crontab -

CMD crond -f -l 2
